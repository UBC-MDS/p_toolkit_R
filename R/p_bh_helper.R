p_bh_helper <- function(pvals, alpha){
  #
  ## Applies Benjamini-Hochberg (BH) correction to the original p-values
  #
  ## Args:
  #     - pvals (numeric): original p-value column index from existing input dataframe
  #     - alpha (numeric): significance level or false discovery rate as a value between 0 and 1
  #
  ## Returns:
  #     - Vector: returns the Benjamini-Hochberg (BH) adjusted p-value
  #

}
